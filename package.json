{
  "name": "@utopian/root",
  "private": true,
  "license" : "GPL-3.0",
  "scripts": {
    "precommit": "lerna run --concurrency 1 --stream precommit",
    "dev": "lerna run dev --stream"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^3.4.3"
  }
}
