@import "~variables"

.project-banner {
  .q-layout-page{
    min-height auto !important
  }

  .project-image {
    width 100%
  }

  .project-description {
    padding 0 20px
    .project-avatar {
      height 45px
      width 45px
      border-radius 50%
    }
    .project-name {
      padding 0px 10px
    }
  }
}