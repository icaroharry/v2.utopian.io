import { routes as core } from '../core'
import { routes as auth } from '../auth'
import { routes as contributions } from '../contributions'
import { routes as projects } from '../projects'
import { routes as accounts } from '../accounts'

export default [core, auth, contributions, projects, accounts]
