export const featuredProjects = ({ featuredProjects }) => featuredProjects
export const project = ({ project }) => project
export const updates = ({ project }) => project && project.updates
export const bounties = ({ project }) => project && project.bounties
